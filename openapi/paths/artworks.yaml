get:
  summary: 작품 목록을 가져옴
  responses:
    "200":
      description: 작품 목록
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/schemas.yaml#/Artwork"
