get:
  description: 작품 목록 취득
  operationId: getArtworks
  tags:
    - Artworks
  responses:
    "200":
      description: 작품 목록이 반환됨
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../components/schemas.yaml#/Artwork"
